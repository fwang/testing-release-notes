name: Changelog

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - env:
          GITHUB_AUTH: ${{ secrets.GITHUB_TOKEN }}
      - run: |
          npm ci
          npx lerna-changelog
